# name: Trigger e2e UI tests

# on: push

# jobs:
#   chrome:
#     runs-on: ubuntu-latest
#     name: trigger in chrome
#     steps:
#       - uses: actions/checkout@v3
#       - uses: cypress-io/github-action@v5
#         with:
#           browser: chrome
#           spec: cypress/e2e/custom_scenarious/newborn.cy.js
name: run on docker image

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    services:
      docker:
       image: cypress/browsers:node-20.9.0-chrome-118.0.5993.88-1-ff-118.0.2-edge-118.0.2088.46-1
       options: --user 1001

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
          spec: cypress/e2e/custom_scenarious/newborn.cy.js
          # spec: docker run -it -v $PWD:/e2e -w /e2e e cypress/browsers:node-20.9.0-chrome-118.0.5993.88-1-ff-118.0.2-edge-118.0.2088.46-1 npm run cy_run_chrome_headed --spec cypress/e2e/custom_scenarious/newborn.cy.js